<UserControl x:Class="EnvBooster.UI.EnvironmentUserControl"
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
  xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
  xmlns:local="clr-namespace:EnvBooster.UI"
  mc:Ignorable="d"
  Background="{StaticResource SurfaceBrush}"           
  MinWidth="350" MinHeight="150">

  <Border Background="{StaticResource BackgroundBrush}" BorderBrush="{StaticResource SecondaryBrush}" BorderThickness="1" CornerRadius="3">

    <Grid>
      <Grid.RowDefinitions>
        <RowDefinition Height="Auto"/>
        <RowDefinition Height="*"/>
      </Grid.RowDefinitions>

      <!-- Header -->
      <Grid Margin="5">
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="*"/>
          <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Column="0" Text="Environment Name" Style="{StaticResource H4}"/>
        <Button Grid.Column="1" Style="{StaticResource PrimaryButton}">
          <Image Source="{StaticResource IconPlayWhite}" Width="16" Height="16"/>
        </Button>
      </Grid>
      
      <!-- Content -->
    </Grid>
  </Border>
</UserControl>
