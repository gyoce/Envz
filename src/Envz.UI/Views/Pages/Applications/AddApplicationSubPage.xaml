<UserControl
  x:Class="Envz.UI.Views.Pages.Applications.AddApplicationSubPage"
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  xmlns:local="clr-namespace:Envz.UI.Views.Pages.Applications"
  mc:Ignorable="d"
  xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
  xmlns:controls="clr-namespace:Envz.UI.Views.UserControls.Controls"
  Width="{StaticResource SubPageWidth}" 
  Height="{StaticResource SubPageHeight}">

  <Border Background="{StaticResource BgPrimaryBrush}">
    <Grid>
      <Grid.RowDefinitions>
        <RowDefinition Height="Auto"/>
        <RowDefinition Height="Auto"/>
        <RowDefinition Height="Auto"/>
        <RowDefinition Height="*"/>
        <RowDefinition Height="Auto"/>
      </Grid.RowDefinitions>

      <TextBlock Grid.Row="0" Style="{StaticResource TextH3}" Text="Add application" HorizontalAlignment="Center" />

      <!-- Path -->
      <Grid Grid.Row="1" Margin="30 20">
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="*"/>
          <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <controls:TextInput Grid.Column="0" Label="Path" Text="{Binding ApplicationPath}" VerticalAlignment="Top" />
        <controls:ButtonText Grid.Column="1" Text="Browse..." Command="{Binding BrowseApplicationCommand}" VerticalAlignment="Bottom" Width="100" Height="{StaticResource TextInputHeight}" Margin="10 0 0 0" />
      </Grid>

      <!-- Bottom buttons -->
      <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0 0 0 20">
        <controls:ButtonText Text="Cancel" ButtonStyle="{StaticResource SecondaryButton}" Command="{Binding CancelAddApplicationCommand}" Width="150" Height="35" Margin="0 0 20 0" />
        <controls:ButtonText Text="Add" Command="{Binding AddApplicationCommand}" Width="150" Height="35" />
      </StackPanel>
    </Grid>
  </Border>

</UserControl>