<UserControl 
  x:Class="Envz.UI.Views.Pages.HomePage" 
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" 
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" 
  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
  xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
  xmlns:vm="clr-namespace:Envz.UI.ViewModels.Pages"
  xmlns:controls="clr-namespace:Envz.UI.Views.UserControls.Controls"
  mc:Ignorable="d" 
  d:DataContext="{d:DesignInstance Type=vm:HomePageViewModel}"
  Width="{StaticResource PageWidth}" 
  Height="{StaticResource PageHeight}">

  <Border Background="{StaticResource BgPrimaryBrush}">
    <Grid>
      <Grid.RowDefinitions>
        <RowDefinition Height="Auto" />
        <RowDefinition Height="*" />
      </Grid.RowDefinitions>

      <controls:Breadcrumb Grid.Row="0" PrimaryMenuText="Home"/>
    </Grid>
  </Border>

</UserControl>