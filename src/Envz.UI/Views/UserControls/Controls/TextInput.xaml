<UserControl 
  x:Class="Envz.UI.Views.UserControls.Controls.TextInput" 
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" 
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" 
  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
  xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
  mc:Ignorable="d" 
  d:DesignWidth="400" 
  d:DesignHeight="75" 
  x:Name="Root">

  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
    </Grid.RowDefinitions>

    <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="5 0 0 3">
      <TextBlock Style="{StaticResource TextBody}" Text="{Binding Label, ElementName=Root, FallbackValue=Label}" />
      <TextBlock Style="{StaticResource TextBody}" Text="{Binding ErrorMessage, ElementName=Root, FallbackValue=Error message}" Margin="10 0 0 0" Foreground="Red" />
    </StackPanel>
    <TextBox Grid.Row="1" Style="{StaticResource TextBoxBody}" Text="{Binding Text, ElementName=Root, UpdateSourceTrigger=PropertyChanged, FallbackValue=Text input}" />
  </Grid>

</UserControl>