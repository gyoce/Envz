<UserControl 
  x:Class="Envz.UI.Views.UserControls.Controls.ButtonText" 
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" 
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" 
  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
  xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
  mc:Ignorable="d"
  x:Name="Root"
  Background="Transparent">

  <Button Command="{Binding Command, ElementName=Root}" 
          Style="{Binding ButtonStyle, ElementName=Root, FallbackValue={StaticResource PrimaryButton}}" 
          Click="Button_Click">
    <TextBlock Text="{Binding Text, ElementName=Root, FallbackValue=Text}" 
               Style="{Binding TextStyle, ElementName=Root, FallbackValue={StaticResource TextBody}}" 
               Foreground="White" 
               VerticalAlignment="Center" />
  </Button>

</UserControl>