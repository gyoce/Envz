<UserControl 
  x:Class="Envz.UI.Views.UserControls.Controls.Breadcrumb"
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
  xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
  xmlns:vm="clr-namespace:Envz.UI.ViewModels.UserControls.Controls"
  mc:Ignorable="d"
  Width="{StaticResource BreadcrumbWidth}" 
  Height="{StaticResource BreadcrumbHeight}"
  x:Name="Root">

  <ItemsControl ItemsSource="{Binding Items, ElementName=Root}" VerticalAlignment="Center" Margin="30 0 0 0">
    <ItemsControl.ItemsPanel>
      <ItemsPanelTemplate>
        <StackPanel Orientation="Horizontal" />
      </ItemsPanelTemplate>
    </ItemsControl.ItemsPanel>

    <ItemsControl.ItemTemplate>
      <DataTemplate>
        <StackPanel Orientation="Horizontal" d:DataContext="{d:DesignInstance Type=vm:BreadcrumbItemViewModel}">
          <TextBlock Text=">" Style="{StaticResource TextH3}" Margin="8 0" Foreground="{StaticResource ForegroundMutedBrush}" Visibility="{Binding IsFirst, Converter={StaticResource InverseBoolToVisibilityConverter}}" />

          <Button Style="{StaticResource BreadcrumbButton}" Command="{Binding NavigateCommand}" Visibility="{Binding IsClickable, Converter={StaticResource BoolToVisibilityConverter}}">
            <TextBlock Text="{Binding Title}" Style="{StaticResource TextH3}" />
          </Button>

          <TextBlock Text="{Binding Title}" Style="{StaticResource TextH3}" Visibility="{Binding IsClickable, Converter={StaticResource InverseBoolToVisibilityConverter}}" />
        </StackPanel>
      </DataTemplate>
    </ItemsControl.ItemTemplate>
  </ItemsControl>

</UserControl>