<UserControl 
  x:Class="Envz.UI.Views.UserControls.Controls.Breadcrumb"
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
  xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
  mc:Ignorable="d" 
  Width="{StaticResource BreadcrumbWidth}" 
  Height="{StaticResource BreadcrumbHeight}"
  x:Name="Root">

  <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
    <Button Margin="30 0 0 0" Command="{Binding PrimaryMenuCommand, ElementName=Root}" Style="{StaticResource BreadcrumbButton}">
      <TextBlock Text="{Binding PrimaryMenuText, ElementName=Root, FallbackValue=PrimaryMenuText}" Style="{StaticResource TextH3}" />
    </Button>
    <TextBlock Text=">" Style="{StaticResource TextH3}" Margin="8 0" Foreground="{StaticResource ForegroundMutedBrush}" />
    <TextBlock Text="{Binding SecondaryMenuText, ElementName=Root, FallbackValue=SecondaryMenuText}" Style="{StaticResource TextH3}" />
  </StackPanel>

</UserControl>