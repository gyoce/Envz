<UserControl 
  x:Class="EnvBooster.UI.Views.Pages.Environments.EditEnvironmentSubPage" 
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" 
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" 
  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
  xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
  xmlns:controls="clr-namespace:EnvBooster.UI.Views.UserControls.Controls"
  mc:Ignorable="d"
  Width="{StaticResource SubPageWidth}" 
  Height="{StaticResource SubPageHeight}">

  <Border Background="{StaticResource BgPrimaryBrush}">
    <Grid>
      <Grid.RowDefinitions>
        <RowDefinition Height="Auto" />
        <RowDefinition Height="*" />
      </Grid.RowDefinitions>

      <TextBlock Grid.Row="0" Style="{StaticResource TextH3}" Text="Edit environment" HorizontalAlignment="Center" />

      <controls:ButtonText Grid.Row="1" Text="Return" Command="{Binding NavigateToHomeEnvironmentsCommand}" Margin="10 0 0 0" HorizontalAlignment="Center" VerticalAlignment="Center" />
    </Grid>
  </Border>

</UserControl>