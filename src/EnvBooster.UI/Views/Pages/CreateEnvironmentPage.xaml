<UserControl x:Class="EnvBooster.UI.Views.UserControls.CreateEnvironmentPage"
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
  xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
  xmlns:local="clr-namespace:EnvBooster.UI.Views.UserControls"
  mc:Ignorable="d"
  Width="1090" Height="480">

  <Border Background="{StaticResource PrimaryBackgroundBrush}">
    <Grid>
      <Grid.RowDefinitions>
        <RowDefinition Height="Auto"/>
        <RowDefinition Height="*"/>
      </Grid.RowDefinitions>

      <TextBlock Grid.Row="0" Text="Create environment page" Style="{StaticResource H2}" HorizontalAlignment="Center"/>

      <Grid Grid.Row="1">
        <Button VerticalAlignment="Center" HorizontalAlignment="Center" Style="{StaticResource PrimaryButton}" Command="{Binding CreateEnvironmentCommand}">
          <TextBlock Text="Create environment" Style="{StaticResource Body}"/>
        </Button>
      </Grid>
    </Grid>
  </Border>

</UserControl>